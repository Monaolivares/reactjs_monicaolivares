import CartIcon from "../../img/cart-icon.png"

const CartWidget = ()=> {
    return(
        <div>
            <img src={CartIcon} alt="cart-widget"></img>
            0
        </div>
    )
}

export default CartWidget